node_modules/
# .env intentionally removed so we can commit configuration when necessary
.DS_Store
*.log
dist/
build/

# Figma exports (contain embedded secrets)
Healthcare Virtual Assistant*

# Local sandbox helper created by the coding agent
.gitconfig_codex
