# Mobile (Expo) app

Run the mobile app (requires Node.js and Expo CLI):

```bash
cd mobile
npm install
npm start
```

Notes:
- Use `expo` to run on Android/iOS emulator or physical device.
- If your backend runs on the same machine, point the app to your machine IP or use tunneling.
